# Synthetic corpus evaluation — dependencies for experiments (bias, privacy, naturalness) and tests.
#
# pip install -r requirements.txt
# With transformers 5.x / recent huggingface-hub, pip may skip downgrades.

numpy>=1.24.0,<3.0.0
scipy>=1.9.0
matplotlib>=3.7.0
tqdm>=4.60.0

# Privacy, naturalness, experiments (perplexity, coherence, memorization, AI detection)
scikit-learn>=1.0.0,<2.0.0
nltk>=3.8.0

# PyTorch + transformers (perplexity 02, optional AI detection). Open upper bound to avoid forced downgrades.
torch>=1.10.0
transformers>=4.20.0

# sentence-transformers (coherence 06, semantic memorization)
sentence-transformers>=2.2.0

# Tests
pytest>=7.0.0
